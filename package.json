{
    "name": "the-301",
    "version": "1.0.0",
    "description": "",
    "main": "index.js",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "start": "parcel *.pug --no-cache --open",
        "build": "rm -rf dist && parcel build index.pug --no-cache --public-url ./"
    },
    "keywords": [],
    "author": "",
    "license": "ISC",
    "devDependencies": {
        "bulma": "^0.9.1",
        "express": "^4.17.1",
        "parcel-bundler": "^1.12.4",
        "pug": "^3.0.0",
        "sass": "^1.27.0"
    },
    "dependencies": {
        "simplelightbox": "^2.2.2"
    }
}
